interface:
  display_name: "Image Creator"
  short_description: "Generate exact-prompt images via imagegen path"
  default_prompt: "Use $image-creator to generate an image through the imagegen generation path without rewriting this prompt, then save the result to the requested path or project root."
