interface:
  display_name: "Image Creator"
  short_description: "Generate images from exact user prompts and save them"
  default_prompt: "Use $image-creator to generate an image from this prompt without rewriting it, then save the result to the requested path or project root."
