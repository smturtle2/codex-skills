interface:
  display_name: "Image Creator"
  short_description: "Rewrite prompts and generate images"
  default_prompt: "Use $image-creator to rewrite my image request into an English, model-friendly prompt, preserve explicit rendered text, constraints, and input-image roles without describing reference image contents into the prompt, write generation instructions first and list input image roles at the end under Input images, load local input images with view_image only immediately before image_gen when needed, pass only the path and never the detail parameter, then state the final prompt and immediately call the image generation path without adding a separate skill-layer safety or censorship-style check, review, or alternative-suggestion step, do not self-critique or auto-regenerate generated output, never call view_image outside that exact bridge step, and save the result to the requested path or project root."
