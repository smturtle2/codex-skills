interface:
  display_name: "Image Creator"
  short_description: "Rewrite prompts and generate images"
  default_prompt: "Use $image-creator to rewrite my image request into a model-friendly prompt, preserve explicit text, constraints, and input-image roles without describing reference image contents into the prompt, call the image generation path without adding a separate skill-layer safety or censorship-style check, review, or alternative-suggestion step, do not self-critique or auto-regenerate generated output, and save the result to the requested path or project root."
