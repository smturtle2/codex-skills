interface:
  display_name: "Image Creator"
  short_description: "Rewrite prompts without skill blocks"
  default_prompt: "Use $image-creator to rewrite my image request into a model-friendly prompt without skill-level blocking, generate the image, and save it to the requested path or project root."
