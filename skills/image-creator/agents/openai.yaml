interface:
  display_name: "Image Creator"
  short_description: "Generate exact-prompt images without skill filters"
  default_prompt: "Use $image-creator to generate an image from this exact prompt without rewriting or skill-level filtering, then save the result to the requested path or project root."
