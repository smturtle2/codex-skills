# 기여하기

각 스킬을 독립적으로 설치할 수 있게 유지하고, 목록을 쉽게 탐색할 수 있게 정리합니다.

[스킬 목록으로](README.ko.md) · [English](CONTRIBUTING.md)

## 스킬 추가

1. `skills/<skill-name>/SKILL.md`를 만들고 YAML 프런트매터에 `name`과 `description`을 작성합니다. 이름은 폴더명과 같은 영문 소문자·하이픈 형식으로 정하고, 설명에는 사용 시점을 명확히 적습니다.
2. 필요한 스크립트·참고 문서·실행용 에셋을 같은 스킬 폴더에 넣습니다. 지침은 간결하게, 보조 스크립트는 로컬에서 검토할 수 있게 작성합니다.
3. 필요하면 `docs/skills/<skill-name>.md`와 `docs/skills/<skill-name>.ko.md`에 사용 가이드를 추가합니다. `# <skill-name>` 제목, 빈 줄, 짧은 요약 문단으로 시작하고 설치 방법·요구 사항·사용 예시·결과물을 설명합니다.
4. 저장소 루트에서 목록을 갱신합니다.

```sh
uv run scripts/update_catalog.py
```

가이드·스크린샷·개별 아이콘은 선택 사항입니다. 가이드가 없는 스킬도 `SKILL.md`의 설명, 바로 복사할 수 있는 설치 명령, 지침 링크로 표시됩니다. 한국어 가이드가 없으면 번역이 추가될 때까지 원문 설명을 사용합니다.

생성 도구가 각 스킬에 같은 규격의 SVG 헤더를 만듭니다. 스킬 폴더에 `assets/icon.svg`가 있으면 사용하고, 없으면 공통 모듈 심볼을 넣습니다. 색상은 스킬 이름으로 정하므로 다른 스킬이 추가·삭제돼도 바뀌지 않습니다.

## 스킬 수정·삭제

스킬 지침과 영향을 받는 사용 가이드를 함께 수정합니다. 가이드가 있으면 해당 언어의 첫 문단을 목록 요약으로 사용합니다.

스킬을 삭제할 때는 해당 폴더, 사용 가이드, 그 가이드에서만 쓰는 에셋을 정리합니다. 다른 스킬의 참조도 필요에 따라 수정한 뒤 목록을 갱신합니다. 목록은 `skills/*/SKILL.md`에서 찾으므로, 이전 가이드가 남아 있어도 삭제된 스킬은 두 README에서 사라집니다.

## 목록 관리

생성 도구는 각 README의 `<!-- skills:start -->`와 `<!-- skills:end -->` 사이와 `docs/assets/catalog/`의 자체 SVG 헤더를 갱신합니다. 더 이상 쓰이지 않는 헤더는 생성 도구의 파일 표시가 있을 때만 삭제하며, 직접 관리하는 에셋은 보존합니다. 소개 등 다른 부분은 직접 편집하고, 자동 생성된 항목은 원본 설명이나 가이드를 수정해 갱신합니다.

영문 가이드에서 설치 프롬프트를 담은 첫 `text` 코드 블록을 두 언어의 README 설치 명령으로 사용합니다. 함께 설치할 스킬도 그대로 반영합니다. 해당 블록이 없으면 단일 스킬 설치 명령을 만듭니다.

파일을 수정하지 않고 목록이 최신 상태인지 확인하려면 다음을 실행합니다.

```sh
uv run scripts/update_catalog.py --check
```

이 명령은 목록과 생성된 헤더의 최신 여부만 확인합니다. 변경된 링크와 관련 스킬 동작은 별도로 검토하고, 기여 내용에 확인한 범위를 적어 주세요.

커밋은 `docs: add a usage guide`, `feat: add a skill`처럼 Conventional Commits 형식을 사용합니다. 관련 없는 변경은 포함하지 않습니다.
